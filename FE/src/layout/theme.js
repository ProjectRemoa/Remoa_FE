const size = {
  mobile: '767px',
  desktop: '768px',
};

const theme = {
  mainColor: 'red',
  subColor: 'blue',
  mobile: `(max-width: ${size.mobile})`,
  desktop: `(min-width: ${size.desktop})`,
};

export default theme;